// $(".hamburger").click(function(){
//     $(".wrapper").toggleClass("active")
//   })

// let btn = document.querySelector("#btn");
// let sidebar = document.querySelector(".sidebar");
// btn.onclick = function () {
//   sidebar.classList.toggle("active");
// };

const typedUse = new Typed(".usePage", {
  strings: [
    
    "La première étape consiste à renseigner la feuille «liste » par le projet, sa nature ainsi que son emplacement ",
    "En ce qui concerne l’emplacement il dépendrait du lieu de la réalisation de la réunion qui peut être dans ce cas soit le siège ou bien le chantier",
  ],
  typedSpeed: 100,
  backspeed: 100,
  backDelay: 2500,
  loop: true,
});
